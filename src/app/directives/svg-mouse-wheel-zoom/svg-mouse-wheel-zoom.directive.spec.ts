import { SvgMouseWheelZoomDirective } from './svg-mouse-wheel-zoom.directive';

describe('SvgMouseWheelZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgMouseWheelZoomDirective();
    expect(directive).toBeTruthy();
  });
});
