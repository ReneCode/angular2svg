import { SvgTransformDirective } from './svg-transform.directive';

describe('SvgTransformDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgTransformDirective();
    expect(directive).toBeTruthy();
  });
});
